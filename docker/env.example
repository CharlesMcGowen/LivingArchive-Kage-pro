# Django Settings
SECRET_KEY=django-insecure-ryu-cybersecurity-key-change-in-production
DEBUG=True
PORT=9000

# PostgreSQL Database Connection (REQUIRED - PostgreSQL is default, no SQLite)
# Set these values to connect to your PostgreSQL server
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres

# Customer Eggs Database (Port 15440)
CUSTOMER_EGGS_DB_NAME=customer_eggs
CUSTOMER_EGGS_DB_PORT=15440

# EggRecords Database (Port 5436)
EGG_DB_NAME=ego
EGG_DB_PORT=5436

# Oak Knowledge Database (Port 5436 - same as eggrecords by default)
OAK_DB_NAME=oak_knowledge
OAK_DB_PORT=5436

